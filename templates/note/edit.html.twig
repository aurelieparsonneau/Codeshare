{% extends 'base.html.twig' %}

{% block title %}Edit Note{% endblock %}

{% block body %}

    <h1 class="text-center">Modifier la Note</h1>
<div class="d-flex flex-col justify-content-center p-6">
    {{ include('note/_form.html.twig', {'button_label': 'Update'}) }}
</div>
<div class="d-flex flex-col justify-content-center">
    <a href="{{ path('app_note_index') }}">back to list</a>

    {{ include('note/_delete_form.html.twig') }}
</div>
{% endblock %}
